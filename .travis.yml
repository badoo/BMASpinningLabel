language: objective-c
osx_image: xcode8.2
script:
- xctool -workspace Example/BMASpinningLabel.xcworkspace -scheme BMASpinningLabel -sdk iphonesimulator build test ONLY_ACTIVE_ARCH=NO GCC_INSTRUMENT_PROGRAM_FLOW_ARCS=YES GCC_GENERATE_TEST_COVERAGE_FILES=YES
after_success:
- bash <(curl -s https://codecov.io/bash)
